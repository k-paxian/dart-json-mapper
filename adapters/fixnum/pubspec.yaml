name: dart_json_mapper_fixnum
version: 1.1.4
description: >
  This is a dart-json-mapper complementary package
  provides support for https://pub.dev/packages/fixnum types in order to
  serialize / deserialize them from / to JSON.
homepage: https://github.com/k-paxian/dart-json-mapper/tree/master/adapters/fixnum
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  dart_json_mapper: '>=2.1.12 <3.0.0'
  fixnum: ^1.0.0
dev_dependencies:
  reflectable:
  lints:
  build_runner:
  build_test:
  test:
#dependency_overrides:
#  dart_json_mapper:
#    path: ../../mapper